package com.github.wkennedy.abi.entry;

/**
 * The AbiType enum represents the different types of ABI entries.
 * The types include constructor, function, event, fallback, receive, and error.
 */
public enum AbiType {
    constructor,
    function,
    event,
    fallback,
    receive,
    error
}
